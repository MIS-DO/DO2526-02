apiVersion: v1
kind: Service
metadata:
  name: flights-api
  namespace: preprod
  labels:
    app: flights-api
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: flights-api-pod
  type: ClusterIP
