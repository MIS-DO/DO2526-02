apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: preprod
  labels:
    app: frontend
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
  selector:
    app: frontend-pod
  type: NodePort
