apiVersion: v1
kind: Service
metadata:
  name: search-api
  namespace: production
  labels:
    app: search-api
spec:
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30004
  selector:
    app: search-api-pod
  type: NodePort
